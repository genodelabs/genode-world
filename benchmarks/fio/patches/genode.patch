--- src/fio/Makefile
+++ src/fio/Makefile
@@ -245,6 +245,9 @@
   LIBS	 += -lpthread -lrt
   LDFLAGS += -rdynamic
 endif
+ifeq ($(CONFIG_TARGET_OS), Genode)
+  SOURCE += os/os-genode.c
+endif
 ifeq ($(CONFIG_TARGET_OS), OpenBSD)
   LIBS	 += -lpthread
   LDFLAGS += -rdynamic
@@ -441,8 +444,6 @@
 endif
 T_TEST_PROGS += $(T_FUZZ_PROGS)
 
-PROGS += $(T_PROGS)
-
 ifdef CONFIG_HAVE_CUNIT
 UT_OBJS = unittests/unittest.o
 UT_OBJS += unittests/lib/memalign.o
@@ -493,7 +494,7 @@
 sharedir = $(prefix)/share/fio
 endif
 
-all: $(PROGS) $(T_TEST_PROGS) $(UT_PROGS) $(SCRIPTS) $(ENGS_OBJS) FORCE
+all: $(PROGS) $(UT_PROGS) $(SCRIPTS) $(ENGS_OBJS) FORCE
 
 .PHONY: all install clean test
 .PHONY: FORCE cscope
--- src/fio/configure
+++ src/fio/configure
@@ -44,7 +44,7 @@
 }
 
 # Default CFLAGS
-CFLAGS="-D_GNU_SOURCE -include config-host.h $CFLAGS"
+CFLAGS="-D_GNU_SOURCE -include config-host.h"
 CONFIGURE_CFLAGS="-Werror-implicit-function-declaration"
 BUILD_CFLAGS=""
 
@@ -211,8 +211,12 @@
   ;;
   --cc=*) CC="$optarg"
   ;;
+  --cross-prefix=*) CROSS_COMPILE="$optarg"
+  ;;
   --extra-cflags=*) CFLAGS="$CFLAGS $optarg"
   ;;
+  --extra-libs=*) LIBS="$LIBS $optarg"
+  ;;
   --build-32bit-win) build_32bit_win="yes"
   ;;
   --target-win-ver=*) target_win_ver="$optarg"
@@ -369,7 +373,7 @@
 elif check_define __QNX__ ; then
   targetos='QNX'
 else
-  targetos=`uname -s`
+  targetos='Genode'
 fi
 
 echo "# Automatically generated by configure - do not modify" > $config_host_mak
@@ -810,7 +810,7 @@
   return 0;
 }
 EOF
-  if compile_prog "" "$LIBS" "posix_aio_fsync" ; then
+  if compile_prog "" "" "posix_aio_fsync" ; then
     posix_aio_fsync=yes
   fi
 fi
--- src/fio/engines/posixaio.c
+++ src/fio/engines/posixaio.c
@@ -85,6 +85,9 @@
 			continue;
 
 		err = aio_error(&io_u->aiocb);
+		if (err < 0)
+			err = errno;
+
 		if (err == EINPROGRESS) {
 			if (suspend_entries < SUSPEND_ENTRIES) {
 				suspend_list[suspend_entries] = &io_u->aiocb;
--- src/fio/os/os-freebsd.h
+++ src/fio/os/os-freebsd.h
@@ -7,7 +7,6 @@
 #include <sys/sysctl.h>
 #include <sys/disk.h>
 #include <sys/endian.h>
-#include <sys/thr.h>
 #include <sys/socket.h>
 #include <sys/param.h>
 #include <sys/cpuset.h>
@@ -19,10 +18,8 @@
 #define FIO_USE_GENERIC_INIT_RANDOM_STATE
 #define FIO_HAVE_CHARDEV_SIZE
 #define FIO_HAVE_FS_STAT
-#define FIO_HAVE_TRIM
 #define FIO_HAVE_GETTID
 #define FIO_HAVE_CPU_AFFINITY
-#define FIO_HAVE_SHM_ATTACH_REMOVED
 
 #define OS_MAP_ANON		MAP_ANON
 
@@ -101,10 +98,7 @@
 
 static inline int gettid(void)
 {
-	long lwpid;
-
-	thr_self(&lwpid);
-	return (int) lwpid;
+	return (int)(intptr_t) pthread_self();
 }
 
 static inline unsigned long long get_fs_free_size(const char *path)
@@ -138,15 +132,4 @@
 #define FIO_MADV_FREE	MADV_FREE
 #endif
 
-static inline int shm_attach_to_open_removed(void)
-{
-	int x;
-	size_t len = sizeof(x);
-
-	if (sysctlbyname("kern.ipc.shm_allow_removed", &x, &len, NULL, 0) < 0)
-		return 0;
-
-	return x > 0 ? 1 : 0;
-}
-
 #endif
